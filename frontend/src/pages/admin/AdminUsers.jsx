import { useEffect,useState } from "react";
import api from "../../api";
export default function AdminUsers(){const [users,setUsers]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{api.get("/admin/users").then(r=>setUsers(r.data.users||[])).catch(e=>setError(e.response?.data?.message||"Unable to load customers.")).finally(()=>setLoading(false))},[]);return <section className="container-shell section-space"><p className="eyebrow">Admin portal</p><h1 className="font-display text-5xl">Customers</h1><p className="text-muted mt-3">View registered customer accounts.</p>{error&&<div className="card p-5 mt-8 bg-red-50 text-red-700">{error}</div>}<div className="card overflow-x-auto mt-10">{loading?<div className="p-8 text-center text-muted">Loading customers…</div>:<table className="w-full text-sm"><thead className="bg-black/5 text-left"><tr><th className="p-5">Name</th><th>Email</th><th>Phone</th><th>Joined</th></tr></thead><tbody>{users.map(x=><tr className="border-t" key={x.id}><td className="p-5">{x.name}</td><td>{x.email}</td><td>{x.phone||"—"}</td><td>{x.createdAt?new Date(x.createdAt).toLocaleDateString():"—"}</td></tr>)}</tbody></table>}</div></section>}
