import{useState}from"react";import{Link,useNavigate}from"react-router-dom";import{useAuth}from"../context/AuthContext";export default function Register(){const[f,setF]=useState({name:"",email:"",password:""}),[err,setErr]=useState(""),{register}=useAuth(),nav=useNavigate();return <section className="min-h-[70vh] grid place-items-center py-16"><form onSubmit={async e=>{e.preventDefault();try{await register(f);nav("/")}catch(x){setErr(x.response?.data?.message||"Unable to register.")}}} className="card p-9 w-[min(100%-32px,440px)]"><Link to="/" className="font-display text-xl">Online Shopping</Link><h1 className="font-display text-4xl mt-12">Create your account</h1><p className="text-muted mt-2 mb-8">Track orders and check out faster.</p><div className="grid gap-4"><label>Name<input className="input mt-2" required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Email<input className="input mt-2" type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><label>Password<input className="input mt-2" type="password" minLength="8" required value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></label>{err&&<p className="text-red-600 text-sm">{err}</p>}<button className="btn btn-dark w-full">Create account</button><p className="text-sm text-center text-muted">Already registered? <Link className="underline text-ink" to="/login">Sign in</Link></p></div></form></section>}
