import{useState}from"react";import{Link,useNavigate}from"react-router-dom";import{useAuth}from"../context/AuthContext";export default function Login(){const[e,setE]=useState(""),[p,setP]=useState(""),[err,setErr]=useState(""),[busy,setBusy]=useState(false),{login}=useAuth(),nav=useNavigate();return <section className="min-h-[70vh] grid place-items-center py-16"><form onSubmit={async x=>{x.preventDefault();setBusy(true);setErr("");try{const u=await login(e,p);nav(u.role==="admin"?"/admin":"/")}catch(x){setErr(x.response?.data?.message||"Unable to sign in.")}finally{setBusy(false)}}} className="card p-9 w-[min(100%-32px,440px)]"><Link to="/" className="font-display text-xl">Online Shopping</Link><h1 className="font-display text-4xl mt-12">Welcome back</h1><p className="text-muted mt-2 mb-8">Sign in to your account.</p><div className="grid gap-4"><label>Email<input className="input mt-2" type="email" required value={e} onChange={x=>setE(x.target.value)}/></label><label>Password<input className="input mt-2" type="password" required value={p} onChange={x=>setP(x.target.value)}/></label>{err&&<p className="text-red-600 text-sm">{err}</p>}<button className="btn btn-dark w-full" disabled={busy}>{busy?"Signing in…":"Sign in"}</button><p className="text-sm text-center text-muted">New here? <Link className="underline text-ink" to="/register">Create an account</Link></p></div></form></section>}
