import{Router}from"express";import bcrypt from"bcryptjs";import User from"../models/User.js";import{protect}from"../middleware/auth.js";import{publicUser}from"../utils/token.js";const r=Router();r.get("/profile",protect,(q,s)=>s.json({user:publicUser(q.user)}));r.put("/profile",protect,async(q,s,n)=>{try{for(const k of ["name","phone","address"])if(q.body[k]!==undefined)q.user[k]=q.body[k];if(q.body.password){if(q.body.password.length<8)return s.status(400).json({message:"Password must be at least 8 characters."});q.user.password=await bcrypt.hash(q.body.password,12)}await q.user.save();s.json({user:publicUser(q.user)})}catch(e){n(e)}});export default r;
