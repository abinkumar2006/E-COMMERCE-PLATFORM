import jwt from "jsonwebtoken";import User from "../models/User.js";
export async function protect(req,res,next){try{const h=req.headers.authorization||"";if(!h.startsWith("Bearer "))return res.status(401).json({message:"Authentication required."});const p=jwt.verify(h.slice(7),process.env.JWT_SECRET);const u=await User.findById(p.id).select("-password");if(!u)return res.status(401).json({message:"User not found."});req.user=u;next()}catch{return res.status(401).json({message:"Invalid or expired token."})}}
export function adminOnly(req,res,next){if(req.user?.role!=="admin")return res.status(403).json({message:"Admin access required."});next()}
